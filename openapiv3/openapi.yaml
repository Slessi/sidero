# generated by protoc-gen-oas. DO NOT EDIT

openapi: 3.1.0
info:
  title: ""
  version: ""
paths:
  /v1/logs:
    get:
      operationId: list
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            format: int32
        - name: offset
          in: query
          schema:
            type: integer
            format: int32
      responses:
        "200":
          description: logs.LogsService.List response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListResponse'
components:
  schemas:
    ListResponse:
      type: object
      properties:
        lines:
          type: array
          items:
            type: string
